<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- Round publication (Android uniquement, retour utilisateur: "on
             oublie tout ce qui est iOS") — vrai App ID AdMob (voir
             src/game/ads.js: APP_ID.android/REAL_APP_ID), à garder en
             synchro avec ce fichier si jamais l'ID change côté console
             AdMob. ios/App/App/Info.plist est resté sur l'ID de test :
             non pertinent tant qu'iOS n'est pas publié. Sans ce meta-data,
             le SDK Google Mobile Ads plante au démarrage. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-4606745726023654~5350590056" />

        <!-- Google Play Games Services (voir strings.xml: games_app_id pour
             le détail du placeholder à remplacer, et playGamesServices.js
             pour le module JS). Requis par PlayGamesSdk.initialize() —
             sans ce meta-data, le SDK ne plante pas mais signIn()/saveGame()
             échouent silencieusement (voir playGamesServices.js). -->
        <meta-data
            android:name="com.google.android.gms.games.APP_ID"
            android:value="@string/games_app_id" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
